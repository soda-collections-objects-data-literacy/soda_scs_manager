security:
  logging:
    sanitize_logs: true
    log_level: 'info'

snapshotPath: '/var/scs-manager/snapshots'
# Host path for Portainer bind mounts. Leave empty when it matches snapshotPath.
snapshotHostPath: ''
