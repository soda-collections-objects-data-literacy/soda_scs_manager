<div class="card mx-auto h-100" style="width: 18rem;">
  <img class="card-img-top p-3 mx-auto d-block" src="{{file_url(imageUrl)}}" alt="{{ title }}" style="height: 150px; max-width: 150px;">
  <div class="card-body text-center d-flex flex-column justify-content-between">
    <div class="soda-scs-manager--card-title"><strong>{{ title }}</strong></div>
    <a href="{{ url }}" class="btn btn-primary mt-auto"><strong>{{ 'MORE INFO'|t }}</strong></a>
  </div>
</div>
